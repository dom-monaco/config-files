Step-by-step Guide for Git Workflow

Pulling Latest Version of Origin
	$ git checkout master
	$ git pull

Creating a Working Branch
	$ git checkout -b new_branch_name

Committing Changes
	$ git status
	$ git add -A
	$ git commit -m "commit message"

Publishing Branch for Review
	$ git push --set-upstream origin new_branch_name

Merging Working Branch onto Master
	$ git checkout master
	$ git pull
	$ git checkout working_branch
	$ git rebase master
Problems might occurs at that point if changes have been done on master while developing on the local branch.
If this happens, git will set HEAD into an orphan branch, highlight the files that have issues and need to be fixes before completing the merge.
To see the conflicting files, use:
	$ git status
If no merge-conflicts or after fixing conflicts
	$ git checkout master
	$ git merge new_branch_name master

	$ git push

Clean Up
	$ git branch -D working_branch
	$ git push origin :working_branch
